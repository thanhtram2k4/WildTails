---
name: User Profile
description: Project owner building WildTails as a thesis project; expects agent-led structured development with clear human approval gates.
type: user
---

The user is the sole implementer and thesis author for WildTails – Catalyst Verse. They are building this as an academic thesis project and need the agent to:

- enforce clear human approval gates (never auto-deploy, never auto-merge, never auto-publish);
- produce ADRs and contracts before implementation;
- flag ambiguity rather than assume;
- output structured phase evidence for thesis documentation.

They operate on Windows 11 with WSL2 and use Claude Code as the primary development workflow tool.
