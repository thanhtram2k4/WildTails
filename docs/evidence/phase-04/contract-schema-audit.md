# Phase 04 Contract and Schema Audit

## Changes Made

### Contracts (`packages/contracts/`)

1. **`knowledge/journal.ts`**: Added `JOURNAL_BODY_MAX_LENGTH = 50_000` and `JOURNAL_MAX_TAGS = 20` constants. Added `.max(JOURNAL_BODY_MAX_LENGTH)` to body field and `.max(JOURNAL_MAX_TAGS)` to tagIds field.

2. **`knowledge/tag.ts`** (NEW): Created `CreateTagRequestSchema` (name: 1-50) and `TagResponseSchema` (id, name, ownerId).

3. **`knowledge/index.ts`**: Added `export * from './tag.js'`.

### OpenAPI (`docs/api/openapi.yaml`)

1. Added `maxLength: 50000` to body in both `CreateJournalRequest` and `UpdateJournalRequest`.
2. Added `maxItems: 20` to tagIds in both request schemas.
3. Added `DELETE /tags/{id}` endpoint.

### Prisma Schema (`packages/database/prisma/schema.prisma`)

1. Added index `[ownerId, folderId]` on Journal.
2. Added index `[ownerId, deletedAt, updatedAt DESC]` on Journal.

### Migration (`packages/database/prisma/migrations/20260804170823_phase04_indexes/`)

1. Two Journal indexes (auto-generated by Prisma).
2. Partial unique index `share_permissions_active_unique` on `(journalId, grantedToUserId) WHERE revokedAt IS NULL` (manually added).

## Consistency Verification

- Zod schemas match OpenAPI schemas for all Phase 04 entities.
- Prisma model field types match Zod/OpenAPI types.
- Visibility enum consistent across Zod, OpenAPI, and Prisma.
- Pagination contract (cursor-based) used consistently.
